#include "main.h"

/**
 * add - adds two numbers together and returns the output
 * @a: first number
 * @b: second number
 * Return: sum of a + b
 */
int add(int a, int b)

{
	int result = a + b;

	return (result);

}
