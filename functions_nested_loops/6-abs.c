#include "main.h"
/**
 * _abs - Print the absolute value of input
 * Return: absolute value of input x
 * @x: the number to be absolutely evaluated
 */
int _abs(int x)
{
	if (x < 0)
		return (-x);
	return (x);
}
