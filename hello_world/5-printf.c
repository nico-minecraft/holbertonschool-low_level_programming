#include <stdio.h>

/**
 * main - This prints the desired text using putss
 * Return: should always be 0
 */
int main(void)
	{
	print("with proper grammar, but the outcome is a piece of art,");
	return (0);
	}
